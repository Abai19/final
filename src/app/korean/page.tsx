'use client';

import {FC} from 'react';

import withHeader from '../../hoc/with-header';

const Korean: FC = () => {
    return (
        <div>korean</div>
    );
};
export default withHeader(Korean);

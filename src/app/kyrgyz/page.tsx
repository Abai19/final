'use client';

import {FC} from 'react';

import withHeader from '../../hoc/with-header';

const Kyrgyz: FC = () => {
    return (
        <div>Kyrgyz</div>
    );
};
export default withHeader(Kyrgyz);
